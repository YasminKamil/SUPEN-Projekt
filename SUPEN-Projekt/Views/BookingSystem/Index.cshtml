@model SUPEN_Projekt.Logic.BookingSystemsViewModel



<p>
    @Html.ActionLink("Skapa nytt bokningssystem", "Create")
</p>
<table class="table">
    <tr>
        <th>
			<h3><b>Företagsnamn</b></h3>
        </th>
        <th>
			<h3><b>Stad</b></h3>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.bookingSystems) {
    <tr>
        <td>
            @Html.RouteLink(item.CompanyName, new { controller = "BookingSystem", action = "BookingSystem", id = item.BookingSystemId }, null)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.City)
        </td>
    </tr>
}

</table>
